[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
filterwarnings = error
markers =
    docker: opt-in execution inside the pinned Docker runner (requires SPECGUARD_DOCKER_TESTS=1)
